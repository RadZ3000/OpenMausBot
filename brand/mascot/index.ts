// Plug a CursorSilhouette-compatible drawing here. Null keeps the bundled
// cursor that Avatar.tsx already ships. `pnpm check:brand --release` fails
// while this directory has no artwork.

export const brandSilhouette = null;
